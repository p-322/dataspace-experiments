{
  "name": "exp-01-hello-connector-api",
  "version": "0.0.1",
  "description": "Hoe maak je van een lege REST-API een dataspace-endpoint? Basisarchitectuur van provider–consumer; contractonderhandeling; policies.“Open by default, hardened by design” — laat zien dat openheid niet hetzelfde is als kwetsbaarheid.",
  "license": "ISC",
  "author": "Gertjan Filarski",
  "type": "module",
  "scripts": {
    "dev": "node --watch src/server.ts",
    "build": "tsc -p tsconfig.json",
    "clean": "docker compose down --volumes --remove-orphans",
    "init": "./scripts/start-containers.sh",
    "experiment": "npm run build && node dist/dataspace-process/run.js",
    "clean-experiment": "npm run clean && npm run init && npm run experiment"
  },
  "dependencies": {
    "fastify": "^5.6.2"
  },
  "devDependencies": {
    "@types/node": "^25.0.3",
    "typescript": "^5.9.0"
  }
}
