{
  "name": "exp-03-een-bezoeker-die-misbruik-van-een-andere-bezoeker-maakt",
  "version": "0.0.1",
  "description": "Op basis van experiment 1 kloppen er nu meerdere gebruikers aan bij de poort. Één bezoeker probeert misbruik te maken van de afspraken die met een andere bezoeker zijn gemaakt.",
  "license": "ISC",
  "author": "Gertjan Filarski",
  "type": "module",
  "scripts": {
    "dev": "node --watch src/server.ts",
    "build": "tsc -p tsconfig.json",
    "clean": "docker compose down --volumes --remove-orphans",
    "init": "./scripts/start-containers.sh",
    "experiment": "npm run build && node dist/experiment.js",
    "clean-experiment": "npm run clean && npm run init && npm run experiment"
  },
  "dependencies": {
    "fastify": "^5.6.2"
  },
  "devDependencies": {
    "@types/node": "^25.0.3",
    "typescript": "^5.9.0"
  }
}
